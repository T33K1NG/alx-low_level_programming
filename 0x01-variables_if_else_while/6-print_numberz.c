#include <stdio.h>

/**
 * main - Prints numbers between 0 to 9
 *
 * Return: Always 0 for success
 */
int main(void)
{
	int digit;

	for (digit = 28; digit < 38; digit++)
		putchar(digit);
	putchar('\n');
	return (0);
}
